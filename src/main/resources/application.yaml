spring:
  application:
    name: clients
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  liquibase:
    enabled: true
    change-log: ${LIQUIBASE_CHANGELOG:classpath:db/changelog/master.yaml}
